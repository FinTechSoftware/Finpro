import { html } from 'lit';
import { ifDefined } from 'lit/directives/if-defined.js';
import { Meta, Canvas, ArgsTable, Story, Preview, Source } from '@storybook/addon-docs';
import { styleMap } from 'lit/directives/style-map.js';
import { map } from 'lit/directives/map.js';
import generalIcons from './general-icon-list';
import cryptoIcons from './crypto-icon-list';

<Meta
  title="Components/Icon"
  component="fp-icon"
  argTypes={{
    name: {
      control: {
        type: 'select',
        options: generalIcons,
        description: 'Name of the icon'
      }
    },
    size: {
      control: {
        type: 'number',
        description: 'Size of the icon (in pixels)',
        min: 8,
        max: 64
      }
    },
    color: {
      control: {
        type: 'color',
        description: 'Color of the icon',
        presetColors: [{
          color: 'var(--fp-color-primary)',
          title: 'Primary'
        },{
          color: 'var(--fp-color-secondary)',
          title: 'Secondary'
        },{
          color: 'var(--fp-color-success)',
          title: 'Success'
        }]
      }
    },
  }}
/>

export const Template = (args) => html`<fp-icon
  name="${args.name}"
  style=${styleMap({'font-size': parseInt(args.size) > 0 ? `${args.size}px` : args.size, 'color': args.color})}></fp-icon>`;

export const SizeVariations = (args) => html`
<fp-icon name="${args.name}" style="font-size: var(--fp-font-size-m)"></fp-icon>
<fp-icon name="${args.name}" style="font-size: var(--fp-font-size-l)"></fp-icon>
<fp-icon name="${args.name}" style="font-size: var(--fp-font-size-xl)"></fp-icon>
<fp-icon name="${args.name}" style="font-size: var(--fp-font-size-2xl)"></fp-icon>
<fp-icon name="${args.name}" style="font-size: var(--fp-font-size-3xl)"></fp-icon>
`

export const ColorVariations = (args) => html`
<fp-icon name="${args.name}" style="color: var(--fp-color-content-primary)"></fp-icon>
<fp-icon name="${args.name}" style="color: var(--fp-color-content-secondary)"></fp-icon>
<fp-icon name="${args.name}" style="color: var(--fp-color-content-tertiary)"></fp-icon>
<fp-icon name="${args.name}" style="color: var(--fp-color-content-passive)"></fp-icon>
`

export const AllIcons = (args) => html`<div
  style="${styleMap({'font-size': `${args.size}px`, 'color': args.color})}">\n${
    generalIcons.map(iconName => html`  <fp-icon name=${iconName} title="${iconName}"></fp-icon> \n`)
  }</div>`;



export const CryptoIcons = (args) => html`<div
  style="${styleMap({'font-size': `${args.size}px`, 'color': args.color})}">\n${
    cryptoIcons.map(iconName => html`  <fp-icon name=${iconName} title="${iconName}"></fp-icon> \n`)
  }</div>`;

# Icon

Icon component is an **internal** component for using inside other Finpro components to show some decorative icons.

<fp-alert variant="warning" icon>Inline styles in examples are only for **demo purposes**. Use regular CSS classes or tag selectors to set styles.</fp-alert>

## Showing Icons

You can show icons by just using `name` attribute.

<Canvas>
  <Story name="Single Icon" args={{ name: 'info' }}>
    {Template.bind({})}
  </Story>
</Canvas>

You can see a list of icons that Finpro provides now in our [Iconography](/docs/design-system-iconography--page) page.

## Sizing Icons

Icon size can be set via CSS with `font-size` property. By default icons are in the size of current font-size of the tag placement.

<Canvas>
  <Story name="Icon sizes" parameters={{
      controls: {
        include: ['name']
      }
    }} args={{ name: 'info' }}>
    {SizeVariations.bind({})}
  </Story>
</Canvas>

## Coloring Icons

Icon color can be set via CSS with `color` property. By default icons are in the color of current tag placement.

<Canvas>
  <Story name="Icon colors" parameters={{
      controls: {
        include: ['name']
      }
    }} args={{ name: 'info' }}>
    {ColorVariations.bind({})}
  </Story>
</Canvas>

## List of general icons

Here we provide a list of all provided icons. You can see them in custom size and colors in Canvas tab.

<Canvas>
  <Story name="General Icon list" parameters={{
      controls: {
        exclude: ['name']
      }
    }} args={{ size: 32 }}>
    {AllIcons.bind({})}
  </Story>
</Canvas>

## List of crypto icons

Here we provide a list of crypto provided icons. You can see them in custom size and colors in Canvas tab.

<Canvas>
  <Story name="Crypto Icon list" parameters={{
      controls: {
        exclude: ['name']
      }
    }} args={{ size: 32 }}>
    {CryptoIcons.bind({})}
  </Story>
</Canvas>

## Reference

<ArgsTable of="fp-icon" />
